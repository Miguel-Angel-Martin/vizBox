export declare interface AvlTranslationService {
  /**
   * The language used for translation.
   */
  language: string;

  /**
   * Get a translation for the key.
   * @param key Key of the translation.
   * @param interpolateParams Parameters to be interpolated into the translated string.
   * @returns The translation in the current [[language]].
   */
  translate(key: string, interpolateParams?: object): Promise<string>;
}
