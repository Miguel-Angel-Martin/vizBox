import { AvlInteractionControl, AvlInteractionControlEventMap } from './avl-interaction-control';

export declare interface AvlFormControl extends AvlInteractionControl {

  /**
   * True if the last call to validate is invalid.
   */
  invalid: boolean;

  /**
   * The name attribute is used to reference the control inside a form.
   */
  name: string;

  /**
   * Set to true to mark the control as required input inside a form.
   */
  required: boolean;

  /**
   * Error message in case form control is invalid.
   */
  errorMessage: string;

}

declare interface AvlFormControlEventMap extends AvlInteractionControlEventMap {

  /**
   * Fired when the invalid property changes.
   */
  'invalid-changed': CustomEvent;

}

