/** @hidden */
export declare class KeyboardNavigator {
    private numberOfItems;
    private gridWidth;
    constructor(numberOfItems: number, gridWidth: number);
    /** @returns to which item to navigate to from the current item and undefined to not move. */
    navigate(currentItemIndex: number, keyPressed: string, nextGroupSize?: number | undefined, prevGroupSize?: number | undefined): number;
}
